{
  "name": "resumable-stream",
  "version": "1.0.0",
  "description": "A resumable stream counter using Cloudflare Workers and Durable Objects",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "durable-objects",
    "sse",
    "stream"
  ],
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
